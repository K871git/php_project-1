# laravel 12+ for the project
# core Auth API, Risk Engine adapter, CLI commands

1) composer create-project laravel/laravel:^12.0 .
2) setting up the env credentials
3) composer require laravel/sanctum

# Install authentication packages:
    For token-based: composer require laravel/sanctum
    For OAuth2 server: composer require laravel/passport